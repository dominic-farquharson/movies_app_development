<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% movies.forEach(function(movie) { %>
      <div>
          <span><%= movie.title %></span>
      </div>
    <% }) %>
  </body>
</html>
